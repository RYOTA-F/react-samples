import { FC } from 'react'
import Router from './router'
import { AppContainer } from './App.style'

const App: FC = () => {
  return (
    <AppContainer>
      <Router />
    </AppContainer>
  )
}

export default App
